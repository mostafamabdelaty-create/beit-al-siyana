# Frontend (Vercel)

## Setup

```bash
npm install
npm run dev
```

## Env

Create `.env` from `.env.example`.

- `VITE_API_BASE_URL` should point to your Railway backend URL in production.

## Build

```bash
npm run build
npm run preview
```
